# Local Airflow runtime state — never commit these
.env
logs/
*.db
*.sqlite
airflow.cfg
standalone_admin_password.txt
simple_auth_manager_passwords.json.generated
__pycache__/
*.pyc

# stray sandbox artifact (safe to delete); ignored so it can't be committed
__deltest.txt
